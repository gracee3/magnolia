[package]
name = "audio_dsp"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib"]

[dependencies]
anyhow = "1.0"
async-trait = "0.1"
magnolia_core = { path = "../../core" }
magnolia_signals = { path = "../magnolia-signals" }
serde_json = "1.0"
