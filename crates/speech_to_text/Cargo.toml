[package]
name = "speech_to_text"
version = "0.1.0"
edition = "2021"

[features]
default = []
sherpa = ["dep:sherpa-onnx"]

[dependencies]
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
sherpa-onnx = { version = "1.13.4", optional = true }
